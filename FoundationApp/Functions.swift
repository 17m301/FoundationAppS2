//
//  Functions.swift
//  FoundationApp
//
//  Created by AFP FED 41 on 15/12/25.
//

import Foundation
import SwiftData
import Charts
import Combine

/*
private var selectedTags: [String] = []
private var bedTime = Date()
@Query private var settings: [UserSettings]

func toggle(_ tag: String) {
    if let idx = selectedTags.firstIndex(of: tag) { selectedTags.remove(at: idx) }
    else { selectedTags.append(tag) }
}

func saveAndContinue() {
    let user = settings.first ?? UserSettings()
    user.sleepTroubles = selectedTags
    user.bedTime = bedTime
    if settings.isEmpty { context.insert(user) }
    onContinue()
}
*/

